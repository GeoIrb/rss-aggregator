---
version: '3'
services:
  database:
    image: "postgres" 
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: "tochka"
      POSTGRES_PASSWORD: "tochka"
      POSTGRES_DB: "tochka"
    volumes:
      - database-data:/var/lib/postgresql/data/ 

volumes:
  database-data: 